# 架构设计与技术亮点

> 本文档用 mermaid 图展示系统架构、核心链路和关键设计决策，配合代码阅读。

## 1. 整体架构

```mermaid
graph TB
    subgraph 客户端
        FE[Next.js 14 前端]
    end

    subgraph 接入层
        NGINX[Nginx 反向代理]
    end

    subgraph 应用层[Spring Boot 应用]
        CTRL[Controller 层]
        INTERCEPT[JWT 拦截器<br/>ThreadLocal]
        RATE[AOP 限流切面<br/>@RateLimit]
        SVC[Service 层<br/>接口 + impl]
        MQ[MQ Producer/Consumer]
        JOB[定时任务<br/>Outbox/超时关单/对账]
    end

    subgraph 缓存层
        L1[Caffeine L1<br/>本地缓存]
        L2[Redis L2<br/>+ Lua + 布隆过滤器]
    end

    subgraph 存储层
        DB[(MySQL 8)]
        RMQ[RocketMQ 5]
    end

    FE --> NGINX --> CTRL
    CTRL --> INTERCEPT --> RATE --> SVC
    SVC --> L1
    L1 --> L2
    L2 --> DB
    SVC --> MQ
    MQ <--> RMQ
    MQ --> DB
    JOB --> DB
    JOB --> RMQ
```

## 2. 抢座核心链路时序

```mermaid
sequenceDiagram
    participant U as 用户
    participant C as Controller
    participant R as 限流切面
    participant S as SeckillService
    participant B as 布隆过滤器
    participant Redis as Redis
    participant MQ as RocketMQ
    participant Cons as MQ Consumer
    participant DB as MySQL

    U->>C: POST /seckill/seize (scheduleId, seats, requestId)
    C->>R: @RateLimit 令牌桶校验
    R-->>C: 放行 / 429
    C->>S: seize(userId, req)
    S->>B: mightContainSchedule(scheduleId)
    B-->>S: false → 返回"场次不存在"
    S->>Redis: EVAL seat_reserve.lua
    Note over Redis: 原子执行:<br/>① requestId 幂等检查<br/>② 座位冲突检查<br/>③ 库存充足检查<br/>④ DECRBY stock<br/>⑤ SET seat lock<br/>⑥ SET reservation 凭证
    Redis-->>S: 1=成功 / -1=座位冲突 / -2=售罄
    S->>MQ: 发送 ORDER_CREATED 消息
    S-->>C: 200 "排队中" (requestId)
    C-->>U: 立即返回
    MQ->>Cons: 投递消息
    Cons->>DB: BEGIN TX
    Cons->>DB: INSERT order (uk_lock_token)
    Cons->>DB: INSERT order_seat
    Cons->>DB: INSERT seat_lock (uk_seat)
    Cons->>DB: UPDATE schedule SET available=available-n WHERE available>=n
    Cons->>DB: INSERT outbox_event
    Cons->>DB: COMMIT
```

## 3. 缓存三大问题解决方案

```mermaid
graph LR
    subgraph 读请求
        Q[查询请求]
    end

    Q --> L1[Caffeine L1<br/>命中?]
    L1 -- 命中 --> R1[返回]
    L1 -- 未命中 --> L2[Redis L2<br/>命中?]
    L2 -- 命中 --> L2CHECK{逻辑过期?}
    L2CHECK -- 未过期 --> R2[回填L1 返回]
    L2CHECK -- 已过期 --> ASYNC[异步重建<br/>@Async + Redisson锁] --> R2
    L2 -- 未命中 --> BLOOM{布隆过滤器<br/>存在?}
    BLOOM -- 不存在 --> NULL[写空值缓存<br/>30s TTL] --> R3[返回null]
    BLOOM -- 存在 --> LOCK[抢Redisson重建锁]
    LOCK -- 抢到 --> DB[查DB] --> PUT[写Redis+L1]
    LOCK -- 未抢到 --> WAIT[短暂等待重读]
```

| 问题 | 方案 |
|---|---|
| 缓存穿透 | 布隆过滤器 + 空值短 TTL |
| 缓存击穿 | 逻辑过期 + 异步重建 + Redisson 锁 |
| 缓存雪崩 | TTL 随机偏移 + L1 兜底 + 接口限流 |

## 4. 三层防超卖

```mermaid
graph TD
    REQ[抢座请求] --> L1[L1: Redis Lua 预扣]
    L1 -- 挡掉99% --> END1[返回排队中]
    L1 -- 通过 --> L2[L2: MySQL 条件更新<br/>WHERE available>=n]
    L2 -- 影响行数=0 --> ROLLBACK[事务回滚]
    L2 -- 影响行数=1 --> L3[L3: seat_lock 唯一索引<br/>uk_schedule_seat]
    L3 -- 重复插入报错 --> ROLLBACK
    L3 -- 成功 --> COMMIT[订单落库]
```

## 5. Outbox 模式

```mermaid
sequenceDiagram
    participant Cons as MQ Consumer
    participant DB as MySQL
    participant Job as OutboxRelayJob
    participant MQ as RocketMQ

    Cons->>DB: BEGIN TX
    Cons->>DB: INSERT order
    Cons->>DB: INSERT outbox_event (status=PENDING)
    Cons->>DB: COMMIT
    Note over DB: 订单和事件同事务<br/>要么都成功要么都失败
    loop 每2秒
        Job->>DB: SELECT * FROM outbox WHERE status=PENDING LIMIT 50
        Job->>MQ: send(message)
        alt 发送成功
            Job->>DB: UPDATE status=SENT
        else 发送失败
            Job->>DB: retry_count++ , 超10次 → DEAD
        end
    end
```

## 6. 订单状态机

```mermaid
stateDiagram-v2
    [*] --> 待支付: 抢座成功
    待支付 --> 已支付: 支付成功
    待支付 --> 已取消: 超时未支付(30min)
    已支付 --> [*]
    已取消 --> [*]: Lua 返还 Redis 库存
```

## 7. 包结构

```
com.wangning.seckill
├── SeckillApplication.java
├── common/           # Result, BizException, ThreadLocal, 常量, JwtUtil
├── config/           # Redis, Redisson, Caffeine, MyBatis, Web, Knife4j, 限流切面
├── controller/       # MVC 的 C
├── service/           # 业务接口
│   └── impl/          # 业务实现
├── mapper/            # MyBatis-Plus 接口
├── entity/ dto/ vo/   # PO / 请求 / 响应
├── lua/               # .lua 脚本
├── mq/                # producer / consumer
├── job/               # Outbox投递 / 超时关单 / 对账
└── cache/             # 多级缓存 / 布隆过滤器
```

## 8. 设计模式应用

| 模式 | 代码位置 | 作用 |
|---|---|---|
| 策略模式 | `RateLimitGranularity` + `RateLimitAspect` | 限流粒度可切换 USER/IP/GLOBAL |
| 模板方法 | `MultiLevelCacheManager.get()` | 定义 L1→L2→回源→回填骨架 |
| 工厂模式 | MQ Consumer 按 eventType 路由 | 不同事件走不同处理 |
| 观察者 | Spring `@TransactionalEventListener` | 订单事件异步触发通知 |
| 代理 | AOP 限流/日志/事务 | 横切关注点 |
| DTO/VO 分离 | dto/ 请求, vo/ 响应 | 不暴露 PO 字段 |
